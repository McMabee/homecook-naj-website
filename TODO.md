# Home Cooking with Naj — to-do list

Check off tasks as they are completed. The site is a static Vite app; its content and the admin login are served by a Supabase project (see the README).

## Before launch

- [ ] **Choose how site content will be published.** For a static launch, update and commit the seed recipes, Instagram tiles, and social settings in `src/store/`, then disable the browser-only admin. If Naj needs to edit the live site, complete the "Production admin work" section below first.
- [ ] **Set up and test the contact form.** Configure `VITE_FORMSUBMIT_ENDPOINT` for Naj's real inbox, complete FormSubmit's activation, and submit a real test enquiry. Confirm that Naj receives it and that the form shows the correct success or error state. (`src/pages/Contact.tsx`, `.env.example`)
- [ ] **Review the public content with Naj.** Confirm the biography, service descriptions and areas, response-time promise, brand-partner names/logos, social links, and every published recipe's ingredients and method. (`src/pages/`, `src/components/BrandPartners.tsx`, `src/store/recipes.ts`)
- [ ] **Replace the four sample Instagram tiles.** Use approved photos, accurate captions, and links to the specific posts; commit the finished tiles to `src/store/instagram.ts` for a static launch. The current tiles link to the profile.
- [ ] **Finalize imagery.** Replace the remaining Unsplash images in the service cards, home-page feature band, and Recipes header with approved images; check crops and loading on mobile. (`src/pages/Home.tsx`, `src/pages/Recipes.tsx`)
- [ ] **Finish metadata once the domain is known.** Add a social preview image and canonical site URL, and update the LocalBusiness `url`, `image`, and `sameAs` values. (`index.html`)
- [ ] **Run a launch review.** Check Home, Recipes, Contact, links, forms, and mobile navigation on phone and desktop; test keyboard navigation and the recipe dialog; run `npm run build` and verify the deployed preview before pointing the domain at it.

## Production admin work (only if Naj must edit the live site)

This section exists because the admin originally saved to `localStorage` through `src/store/storage.ts`, inlined uploaded photos as base64 data URLs inside that JSON, and gated itself with `VITE_ADMIN_PASSWORD`, a value that shipped in the public JavaScript. Skip the whole section if Naj is happy to send content changes to a developer: the static path in "Before launch" is less work and has nothing that can break at 2 a.m.

The plan uses Supabase (Postgres, Auth, and file Storage in one hosted project). The code steps (6 to 9) were completed on 2026-09-14; the README's Stack, Configuration and admin sections describe how the pieces fit. Do the steps in order; each depends on the one before it unless noted. Steps 1 to 5 are dashboard work with no code, 6 to 9 are code, 10 and 11 are verification, 12 and 13 are deployment and handover. Once it is done, Naj can do the Instagram-tile and recipe-review items from "Before launch" in the admin instead of through code, but keep the in-code seed data presentable anyway because it is the fallback when the backend is unreachable.

### Set up the backend (Supabase dashboard, no code)

~~1. [x] **Confirm the decision and who owns the account.** Agree with Naj that she will edit the~~ live site herself. Create the Supabase account under an email Naj controls, or under an agency account that can be transferred to her, so the site's content is never tied to a developer's personal login.

~~2. [x] **Create the Supabase project and record its keys.** Choose the Canada (Central) region,~~ closest to Ancaster, and a strong database password. Record three things in a password manager: the project URL (becomes `VITE_SUPABASE_URL`), the *publishable* key (`sb_publishable_…`, becomes `VITE_SUPABASE_PUBLISHABLE_KEY`; it is safe in the browser because row-level security does the protecting), and the database password (only for the Supabase CLI or direct SQL access; never give it a `VITE_` prefix and never put it in the deploy environment). Do not copy the *secret* key (`sb_secret_…`) anywhere near this repo; the site does not need it.

~~3. [x] **Create Naj's login and close sign-ups.** Under Authentication, add one user with~~ Naj's email and a strong password with "auto confirm" ticked, then turn off "Allow new users to sign up" so nobody else can create an account. Add `http://localhost:5173` to the redirect allow-list now so password-reset emails work in development; the production Site URL is set in step 12 once the domain is known. **Checked 2026-09-14:** sign-ups are now closed. `/auth/v1/settings` returns `disable_signup: true`, and an anonymous `POST /auth/v1/signup` with the publishable key gets HTTP 422 `signup_disabled` (no user is created).

~~4. [x] **Create the content table with row-level security.** One table, `site_content`, keyed~~ by the three keys the stores already use (`chef_recipes`, `naj_instagram_posts`, `naj_settings`), so `readJSON`/`writeJSON` keep their meaning and the stores barely change:

   ```sql
   create table public.site_content (
     key        text primary key,
     value      jsonb not null,
     updated_at timestamptz not null default now()
   );
   alter table public.site_content enable row level security;
   create policy "public read" on public.site_content
     for select to anon, authenticated using (true);
   create policy "owner write" on public.site_content
     for all to authenticated using (true) with check (true);
   ```

   Because sign-ups are closed, "authenticated" means Naj. If anyone else ever gets a login, tighten the write policy to a list of allowed `auth.uid()` values. No seeding step is required: when a row is missing the store returns the in-code seed exactly as it does today, and Naj's first save writes the full list. Writes replace the whole list, which is fine for one editor and a dozen recipes; one row per recipe in a proper `recipes` table is the upgrade path if the list ever grows large.

~~5. [x] **Create a public Storage bucket for photos.** Bucket `images`, public read, with a~~ per-file size limit (2 MB is plenty after the browser downscale) and allowed types restricted to JPEG, PNG, and WebP. Add policies on `storage.objects` scoped to that bucket: anyone may select, only `authenticated` may insert, update, or delete. Fix the path convention now: `recipes/<recipe-id>-<timestamp>.jpg` and `instagram/<post-id>-<timestamp>.jpg`.

### Change the code

~~6. [x] **Wire the environment and a single client.** *Done 2026-09-14: `src/lib/supabase.ts`~~, `src/vite-env.d.ts`, and `.env`; `.env.example` was left as a placeholder on request.* Fix `.env.example` (the two Supabase names currently have no `=` sign or comment, and `DATABASE_PASSWORD` should be removed or marked CLI-only), add the two values to `.env`, declare them in `src/vite-env.d.ts`, and create `src/lib/supabase.ts` that builds one client from them. When the variables are missing, the public pages should still render the in-code seed and the admin should show a "not configured" message, the same way it handles a missing password today.

~~7. [x] **Make the stores asynchronous and load content once.** *Done  2026-09-14:~~ `src/store/storage.ts` selects and upserts `site_content`; `src/store/content.tsx` holds `ContentProvider`, `useContent()` and `useSettings()`; the admin refuses to save while the live content failed to load; `StorageFullError` and the `localStorage` path are gone.* Change `readJSON`/`writeJSON` in `src/store/storage.ts` to select and upsert `site_content` rows, so `getRecipes`, `saveRecipe`, `deleteRecipe`, `getInstagramPosts`, `saveInstagramPosts`, `getSettings`, and `saveSettings` return Promises. Every consumer calls them synchronously inside `useState` today, so each needs a loading and an error state: `src/pages/Recipes.tsx`, `src/components/InstagramFeed.tsx`, `src/components/Footer.tsx`, `src/pages/Contact.tsx` (all through `useSettings` or `getRecipes`), and the three panels in `src/pages/admin/`. The cleanest shape is a `ContentProvider` mounted in `src/App.tsx` that fetches all three rows in parallel once, shows a light placeholder for the recipe grid and Instagram tiles until they arrive, and falls back to `DEFAULT_SETTINGS` and the seed data if the request fails. Delete `StorageFullError` unless a `localStorage` fallback is kept on purpose.

~~8. [x] **Upload photos to Storage instead of inlining them.** *Done 2026-09-14:~~ `downscaleImage` and `uploadImage` in `src/lib/image.ts`; `ImageField` takes `folder`, `name` and `onBusyChange`. Replaced photos are not deleted from the bucket (noted in the README).* `fileToDataUrl` in `src/lib/image.ts` returns a base64 data URL that is stored inside the content JSON; in a database that would bloat every visitor's page load. Keep the browser downscale (the 900 px limit can rise to about 1600 px now that browser storage is not the constraint) but return a `Blob`, upload it to the `images` bucket under the step 5 path convention, and store the public URL in `imageUrl`. Always upload to a new object name rather than overwriting, so the CDN never serves a stale photo. `ImageField` in `src/pages/admin/ui.tsx` needs an uploading state and an error message, and the save buttons in the panels should be disabled while an upload is in flight. Deleting the old object when a photo is replaced or a recipe is removed is a nice-to-have; note it in the README if skipped.

~~9. [x] **Replace the bundled password with Supabase Auth.** *Done 2026-09-14: `useAuth()`~~ in `src/lib/auth.ts` owns the session and the recovery flag; `Admin.tsx` has sign-in, forgot-password and new-password screens; `VITE_ADMIN_PASSWORD` is gone from `.env`, the typings and the README (`.env.example` still lists it).* In `src/pages/admin/Admin.tsx` the login form should call `signInWithPassword`, `authed` should come from `getSession()` plus `onAuthStateChange`, and Log out should call `signOut()`. Add a "Forgot password" link that calls `resetPasswordForEmail`; the emailed link returns to the site with a token in the URL and fires a `PASSWORD_RECOVERY` auth event, so `App.tsx` must open the admin with a "set new password" form when that event arrives (the site has no router, so this is the one place the URL matters). Then remove `VITE_ADMIN_PASSWORD` from `.env`, `.env.example`, `src/vite-env.d.ts`, and the README, and delete the "changes are saved in this browser only" notice in `Admin.tsx`. Remember that the client-side `authed` flag is only a UI convenience; the policies from steps 4 and 5 are the real protection, which is what step 10 checks.

### Verify before anyone relies on it

~~10. [x] **Prove an anonymous visitor cannot write.** With only the project~~ URL and publishable key and no session, attempt from `curl` or a browser console: an upsert to `site_content`, an upload to `images`, and a `signUp` call. The upsert and upload must be rejected (HTTP 401 or 403 with a row-level security error) and sign-up must fail because sign-ups are disabled. Note that an anonymous *update* filtered out by a policy returns success with zero rows changed rather than an error, so re-read the row afterwards and confirm it is unchanged. Also confirm that no real key appears after `npm run build` (`grep -oE "sb_secret_[A-Za-z0-9_-]{20,}" dist/assets/*.js` must print nothing; the bare `sb_secret_` prefix is part of supabase-js's own key check), and that Supabase's Security Advisor reports no tables without row-level security. *Partly checked 2026-09-14: an anonymous upsert to `site_content` got 401 with code 42501, an anonymous upload to `images` got 403, the table was unchanged afterwards, and the bundle holds only the URL and publishable key. The `signUp` check passed later that day: HTTP 422 `signup_disabled`, no user created. Still to do: the Security Advisor (dashboard only).*

11. [ ] **Test content recovery end to end and document backups.** Signed in as Naj in one browser: add a recipe with an uploaded photo, edit it, reorder the Instagram tiles, change the Facebook URL, delete a recipe, then reload, open the site on a phone, and confirm every change is visible there without logging in. Sign out and confirm the admin is closed. Then write the backup procedure into the README: the free tier has no automatic database backups, so add a small script (for example `npm run content:export`) that saves the three `site_content` rows to dated JSON files, plus a note on downloading the `images` bucket from the dashboard, and the restore procedure (upsert the JSON back, re-upload photos, and the in-code seed is the last resort). Take one export after Naj's first real editing session. *Partly done 2026-09-14: `npm run content:export`, `npm run images:export` and `npm run content:restore -- <file>` are in `scripts/`, and the README's "Content backup and recovery" section has the backup and restore steps. The first export is in `backups/content/`; it holds 0 rows, and the bucket listed 0 files, because nothing has been saved from the admin yet (if photos have in fact been uploaded, check the bucket's anonymous select policy from step 5). The JSON exports are meant to be committed; `backups/images/` is gitignored. Still to do: the signed-in editing test, the phone check, the sign-out check, and one export after Naj's first real session.*

### Deploy and hand over

12. [ ] **Configure the host and the domain-dependent settings.** Add `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY` to the hosting provider's build environment (Vite bakes them in at build time) and remove `VITE_ADMIN_PASSWORD` there. Once the domain is known, set the Supabase Site URL and redirect allow-list to it (step 3) and retest the password-reset email against the deployed site. Decide what to do about free-tier pausing: Supabase pauses free projects after about a week with no API requests, which a live site with visitors normally prevents but a very quiet one may not. Either accept it (the fallback from step 7 keeps the site up on seed data), schedule a weekly ping, or move to the paid tier.

13. [ ] **Update the docs and brief Naj.** Rewrite the README's "Content and the admin" section, its Configuration table, and "Before going live" items 2 and 3, which no longer apply. Give Naj a one-page guide: where the admin is, how to sign in and reset her password, how to add or edit a recipe and a tile, recommended photo sizes, that saves are live immediately and there is no undo, that she should keep only one admin tab open because the last save wins, and whom to contact. Only then tick "Choose how site content will be published" in the launch list.

## After launch

- [ ] **Give Recipes and Contact their own URLs** if direct links, browser back/forward, and separate search results are important. Page changes currently happen only in React state. (`src/App.tsx`)
- [ ] **Add a small set of browser smoke tests** for navigation, recipe viewing, and contact-form success/error behavior once the launch workflow is stable.
- [ ] **Check real-device performance and accessibility** after deployment, then address any measured issues with images, contrast, focus, or loading.
